{
  "name": "la-toy-district",
  "private": true,
  "type": "module",
  "scripts": {
    "pipeline": "node pipeline/01-opendata.mjs && node pipeline/02-places.mjs && node pipeline/03-crossmatch.mjs && node pipeline/04-enrich.mjs && node pipeline/05-build.mjs",
    "dev": "npm --prefix web run dev",
    "build": "npm --prefix web run build"
  },
  "devDependencies": {
    "zod": "^3.24.0"
  }
}
